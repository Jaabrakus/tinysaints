# Server-side only. Never expose this key in client code.
MOONSHOT_API_KEY=

# The provider is OpenAI-compatible, so these can be swapped later.
AI_BASE_URL=https://api.moonshot.ai/v1
AI_MODEL=kimi-k3
